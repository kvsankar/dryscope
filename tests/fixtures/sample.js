export function greet(name) {
  return `Hello, ${name}!`;
}

const double = (x) => {
  return x * 2;
};

class Calculator {
  add(a, b) {
    return a + b;
  }

  subtract(a, b) {
    return a - b;
  }
}
